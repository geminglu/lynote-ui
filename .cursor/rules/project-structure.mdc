---
alwaysApply: true
description: GML UI 项目结构和架构指南
---

# GML UI 项目结构指南

这是一个基于 React + TypeScript + Tailwind CSS 的组件库项目，采用 monorepo 架构。

## 项目架构

### 根目录结构

- `packages/` - 所有包的主目录
- `app/` - 应用目录（包含 docs 和 storybook）
- `build/` - 构建脚本
- 配置文件：`package.json`, `pnpm-workspace.yaml`, `tsconfig.json`, `eslint.config.js`

### 包结构 (`packages/`)

#### `components/` - UI 组件包

所有组件统一放在 `packages/components/src/` 目录下，每个组件是一个子目录：

- `packages/components/` - 组件包根目录
  - `src/` - 所有组件源码
    - `button/` - 按钮组件
    - `input/` - 输入框组件
    - `form/` - 表单组件（包含子组件和 hooks）
    - `checkbox/` - 复选框组件
    - `radio-group/` - 单选框组组件
    - `label/` - 标签组件
    - `hooks/` - 共享的 hooks（如 `use-mobile.ts`）
    - `style/` - 全局样式
    - `index.ts` - 统一导出所有组件
  - `index.ts` - 包入口文件
  - `package.json` - 包配置

#### `core/` - 核心功能包

- `lib/` - 工具库包 (`@lynote/utils`)
  - `src/` - 工具函数源码
    - `utils.ts` - 工具函数（如 `cn`）
    - `cva.ts` - CVA 相关工具
    - `index.ts` - 导出文件
- `icon/` - 图标系统包 (`@lynote/icon`)
  - `src/icons/` - 图标组件
  - `src/index.ts` - 导出文件

### 组件标准结构

每个组件位于 `packages/components/src/component-name/` 目录下：

```text
component-name/
├── index.tsx          # 组件主文件（包含组件实现和导出）
├── demos/             # 演示文件目录
│   ├── base.tsx      # 基础用法示例
│   ├── variant.tsx   # 变体示例（可选）
│   └── ...           # 其他示例
└── README.md         # 组件文档
```

对于复杂组件（如 `form`），可能包含子目录：

```text
form/
├── components/        # 子组件
│   ├── Form.tsx
│   ├── FormControl.tsx
│   ├── FormField.tsx
│   └── ...
├── context/          # 上下文（可选）
│   └── index.ts
├── hooks/            # 自定义 hooks（可选）
│   └── useFormField.ts
├── demos/            # 演示文件
├── type.ts           # 类型定义（可选）
├── index.ts          # 导出文件
└── README.md         # 文档
```

### 应用目录结构 (`app/`)

- `docs/` - 文档站点（使用 dumi）
  - `src/` - 文档源码
  - `docs/` - Markdown 文档
  - `.dumi/` - dumi 配置
- `storybook/` - Storybook 演示
  - `src/stories/` - Storybook 故事文件
  - `.storybook/` - Storybook 配置

## 技术栈

- **框架**: React 19
- **语言**: TypeScript
- **样式**: Tailwind CSS 4.x
- **构建**: Vite
- **包管理**: pnpm workspace
- **图标**: Lucide React
- **文档**: dumi (docs) + Storybook

## Git 工作流

### 提交前检查

- 使用 Husky 管理 Git hooks
- lint-staged 自动格式化并检查代码
- 检查范围: `**/*.{js,jsx,ts,tsx}`
- 所有 commit message 必须使用中文简体。
  - 格式：`<type>(<scope>): <subject>`
  - 示例：
    - feat(blog): 添加文章编辑功能
    - fix(auth): 修复登录验证问题
    - docs: 更新 README 文档

### 提交规范

- 提交前自动运行 Prettier 格式化
- 提交前自动运行 ESLint 检查并修复
