---
globs: package.json,pnpm-workspace.yaml
description: 包管理和构建配置规范
---

# 包管理和构建规范

## 包命名规范

### 内部包命名

- 格式：`lynote-ui`
- 组件包：`lynote-ui`
- 核心包：`@lynote/utils`, `@lynote/icon`

### 版本管理

- 使用语义化版本控制
- 组件包版本：`0.0.1` (开发阶段)
- 核心包版本：`1.0.0` (稳定版本)

## Package.json 配置

### 标准包配置结构

```json
{
  "name": "lynote-ui",
  "version": "0.0.1",
  "description": "",
  "source": "./src/index.ts",
  "main": "./src/index.ts",
  "module": "./src/index.ts",
  "publishConfig": {
    "main": "./dist/index.js",
    "module": "./dist/index.mjs",
    "types": "./dist/index.d.ts",
    "exports": {
      ".": {
        "import": {
          "types": "./dist/index.d.mts",
          "default": "./dist/index.mjs"
        },
        "require": {
          "types": "./dist/index.d.ts",
          "default": "./dist/index.js"
        }
      }
    }
  },
  "files": ["dist", "README.md"],
  "peerDependencies": {
    "react": ">=19.0.0-rc.0",
    "react-dom": ">=19.0.0-rc.0",
    "tailwindcss": "^4.1.3"
  }
}
```

### 依赖管理

- **peerDependencies**: React, React-DOM, Tailwind CSS
- **dependencies**: 内部包使用 `workspace:*`
- **devDependencies**: 构建工具和开发依赖

```json
{
  "dependencies": {
    "lynote-ui": "workspace:*"
  }
}
```

## 构建配置

### 构建脚本 ([build/index.js](mdc:build/index.js))

- 使用 Vite 进行快速构建
- 支持 CJS 和 ESM 双格式输出
- 外部化 `@lynote/*` 依赖

### 构建输出结构

```text
dist/
├── index.js        # CommonJS 格式
├── index.mjs       # ES Module 格式
├── index.d.ts      # TypeScript 声明文件
├── index.d.cts     # CommonJS 声明文件
└── index.js.map    # Source map
```

## Workspace 配置

### pnpm-workspace.yaml

```yaml
packages:
  - "packages/**/**"
  - "app/**/**"
```

### 根目录脚本

- `build`: 构建所有包
- `lint`: 代码检查
- `lint:fix`: 自动修复代码格式
- `prepare`: 安装 husky

## 开发工具配置

### ESLint 配置

- 使用 flat config 格式
- 集成 TypeScript, React, Prettier
- 支持 Storybook 文件

### Lint-staged 配置

```json
{
  "lint-staged": {
    "packages/**/*.{js,jsx,ts,tsx}": [
      "eslint --fix",
      "prettier --write"
    ],
    "app/**/*.{js,jsx,ts,tsx}": [
      "eslint --fix",
      "prettier --write"
    ]
  }
}
```

## 发布配置

### 发布前检查

1. 确保所有包都有正确的 `publishConfig`
2. 检查 `files` 字段包含必要文件
3. 验证 peerDependencies 版本兼容性
4. 运行构建脚本确保无错误

### 版本发布流程

1. 更新包版本号
2. 运行 `pnpm build`
3. 运行 `pnpm lint`
4. 发布到 npm registry
